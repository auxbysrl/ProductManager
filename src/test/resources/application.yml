server:
  port: 8090
spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    show-sql: true
  datasource:
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      max-lifetime: 800000 #

aws:
  region: test
  bucket: test
  accessKey: test
  secretKey: test
  endpoint: test
